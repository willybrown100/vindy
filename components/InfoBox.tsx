import { Image, StyleSheet, Text, View } from 'react-native'
import React from 'react'
interface inFoType {
  title:string,
  avatar:string
}
const InfoBox = ({title}:inFoType) => {
  return (
    <View>
  
      <Text>{}</Text>
    </View>
  )
}

export default InfoBox

const styles = StyleSheet.create({})